[project]
name = "distraction-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mediapipe>=1.0.0",
    "opencv-python>=5.0.0.93",
    "torch>=2.11.0",
    "torchvision>=0.26.0",
    "imageio-ffmpeg>=0.4.9",
    "ultralytics>=8.2.0",
    "pillow>=10.0.0",
]
[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = [{ index = "pytorch-cu128" }]
torchvision = [{ index = "pytorch-cu128" }]
